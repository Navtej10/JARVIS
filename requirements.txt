# --- V1: core hand tracking + mouse control ---
opencv-python>=4.9.0
mediapipe>=0.10.9
pyautogui>=0.9.54
numpy>=1.26.0
pynput>=1.7.6          # lower-level input, useful alongside/instead of pyautogui on Windows

# --- V2: window management ---
pywin32>=306; sys_platform == 'win32'

# --- V3: Python <-> React bridge ---
websockets>=12.0
pydantic>=2.6.0          # schema validation for bridge messages and AI actions

# --- V5: voice + AI intent ---
openai-whisper>=20231117   # local speech-to-text (swap for cloud STT if preferred)
anthropic>=0.34.0

# --- V6: depth ---
pyrealsense2>=2.55.1; sys_platform != 'darwin'   # Intel RealSense SDK bindings

# --- dev/testing ---
pytest>=8.0.0
python-dotenv>=1.0.1
